CREATE TABLE forum (
  forum_id INT PRIMARY KEY AUTO_INCREMENT,
  forum_detail TEXT NOT NULL,
  title VARCHAR(255) NOT NULL,
  forum_date DATETIME NOT NULL,
  user_id INT NOT NULL,
  FOREIGN KEY (user_id) REFERENCES user(user_id)
);